import { View } from "@react-three/drei"


const ModelView = ({index,item,size,groupRef,controlRef,gsapType,setRotationstate}) => {
  return (
   <View 
   index={index}>


   </View>
  )
}

export default ModelView